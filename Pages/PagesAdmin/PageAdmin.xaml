<Page x:Class="Card_management_system.Pages.PagesAdmin.PageAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Card_management_system.Pages.PagesAdmin"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="400"
      Title="Админ меню">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="../../Resource/Background/BackgroundImage3.png"/>
        </Grid.Background>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="50">
            <Button Name="buttonClientPage"
                    Content="Личный кабинет"
                    Width="170"
                    Style="{StaticResource buttonMainStyle}"
                    Click="buttonClientPage_Click"/>
            <Button Name="buttonListOfCards"
                    Margin="10"
                    Content="Список пользователей"
                    Style="{StaticResource buttonMainStyle}"
                    Click="buttonListOfCards_Click"/>
        </StackPanel>
    </Grid>
</Page>
